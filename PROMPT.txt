Fix import page JS syntax error + remove premature auto-share on Import click:

Syntax error: Import page throws Uncaught SyntaxError: Unexpected token ';' (at import:99:33) on load. Locate and inspect line 99 of the import page's JS (likely editor-file-import.js or equivalent import handler) and fix the syntax error at its source — don't work around it.
Wrong behavior — Import button currently auto-shares: Clicking Import currently calls the share/save endpoint directly and shares the snippet/file immediately. This is wrong for both text and binary files. Locate the exact code path the Import button click handler currently calls, and remove the premature save/share call from it.
Correct flow for text/code files: On Import click, if the file is text/code, only render it into the editor — inject via the existing CodeMirror full-doc dispatch and redirect to /editor. Do not call any save/share endpoint at this stage. Syntax highlighting is already handled by the existing editor Compartment logic — don't touch it. Sharing only happens afterward, when the user clicks the existing Share button on the editor page, using the flow that already exists for code snippets. Do not write a new share path for this.
Correct flow for non-text/binary files: On Import click, render into the existing full-screen black preview interface (already built for file preview) — not the modal. Add spacing of ~50–100px margin on each side so it doesn't render edge-to-edge. Do not call any share/save endpoint here either — import only renders.
Constraint: Do not invent new architecture, new endpoints, or new methods for anything that already exists — reuse the existing editor-render dispatch, the existing Share button/flow on the editor page, and the existing black preview interface. This is a bug fix (removing a wrongly-placed auto-share call and fixing a syntax error), not a redesign.
One more addition — surface the actual failure reason on upload errors:

Error messaging on size/validation failure: When a file exceeds the max size limit, the backend correctly returns a 400 Bad Request — that part is right. But the UI currently just shows a generic "Upload failed" message regardless of cause. Fix the frontend error handler to read the actual reason from the 400 response body (e.g. "file exceeds size limit" / "unsupported file type") and display that specific message to the user, not a generic failure string. Check what error detail the backend already sends back in the 400 response — reuse that message, don't invent new error text or a new response shape on the backend if one already exists. If the backend isn't currently sending a specific reason in the response body, that's the one place a small addition is needed — add a clear detail message per failure case (size vs type) so the frontend has something real to display.